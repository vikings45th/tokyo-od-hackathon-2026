/**
 * ヒーローの図版。ランドセルの線画。
 *
 * なぜランドセルか：日本で「小1」を一語で指す記号で、h1（保育園には、入れた。／
 * 小1の壁は、どこにある？）と噛み合う。時計やカレンダーだと、すぐ下の年表（.tl）と役割が重なる。
 *
 * 🔴 作法（ここを崩すとクリップアートに見える）：
 *   - 塗りは使わない。細線だけで描く
 *   - 色は currentColor。テーマ切替に自動で追従する
 *   - 差し色は1か所だけ（--accent の錠前）。グラデーション・影・角丸カードは使わない
 *   - 外部画像は使わない（ビルドがネットに触れない：NFR-4）
 *
 * 形の要点：**かぶせ（フラップ）を大きく取る**こと。ここが小さいと、ただの鞄に見える。
 * 持ち手・肩ベルト・差し込み錠の3つが揃って初めてランドセルとして読める。
 */
export function HeroMark() {
  return (
    <svg
      className="heromark"
      viewBox="0 0 200 200"
      role="img"
      aria-label="ランドセルのイラスト"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
    >
      {/* 肩ベルト（背面）。本体の上端から短く出るだけ。長く伸ばすと触角に見える */}
      <g opacity={0.55}>
        <path d="M68 70 C59 58 57 46 61 38" />
        <path d="M132 70 C141 58 143 46 139 38" />
        <path d="M55 37 h12" />
        <path d="M133 37 h12" />
      </g>

      {/* 持ち手 */}
      <path d="M89 66 a11 9 0 0 1 22 0" />

      {/* 本体。🔴 縦長にする。横長だとブリーフケースに見える（実物は約 23×31cm） */}
      <rect x="56" y="66" width="88" height="104" rx="12" />

      {/* かぶせ（フラップ）。本体の6割を覆う。ここが浅いとただの鞄になる */}
      <path d="M56 78 a12 12 0 0 1 12 -12 h64 a12 12 0 0 1 12 12 v42 a10 10 0 0 1 -10 10 h-68 a10 10 0 0 1 -10 -10 z" />
      {/* かぶせのステッチ */}
      <path
        d="M65 79 a4 4 0 0 1 4 -4 h62 a4 4 0 0 1 4 4 v38 a4 4 0 0 1 -4 4 h-62 a4 4 0 0 1 -4 -4 z"
        opacity={0.3}
      />

      {/* 差し込み錠。かぶせの下端をまたぐ。ここだけ差し色 */}
      <rect x="92" y="123" width="16" height="15" rx="2" stroke="var(--accent)" />
      <path d="M100 138 v7" stroke="var(--accent)" />

      {/* 前ポケットの口 */}
      <path d="M66 153 h68" opacity={0.3} />
    </svg>
  );
}
