# 秘密情報 — 絶対にコミットしない
.env
.env.*
!.env.example
*.pem
*.key
.dev.vars

# 依存関係
node_modules/

# ビルド成果物
dist/
build/
.wrangler/
.output/

# エディタ・OS
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code のローカル設定（.claude/settings.json は共有するのでコミットする）
.claude/settings.local.json

# ログ
*.log
npm-debug.log*

# 取得した元データ（data/README.md 参照）
# raw は転載不可のファイルを含むのでコミットしない。scripts/fetch_raw.py で取り直す
data/raw/
# 学童クラブ名簿はオープンデータカタログ未掲載＝転載不可。ライセンス確認が済むまで除外
data/processed/gakudou_clubs_*.csv
# 福祉局サイトの元データ。data/raw/ ではなく data/ 直下に置かれた分も転載不可なので除外
# （d2b600c で誤ってコミットされていた。ファイル自体はローカルに残す）
data/*.pdf
data/20251001.csv

# 別リポジトリ（startup/.git を持つ）。ここで追跡すると壊れた gitlink になる（docs/17 依頼C）
startup/
