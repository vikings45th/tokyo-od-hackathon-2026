# やることリスト（8/20 → 8/31 逆算）

**作品提出締切は 8/23（日）17:00。実装に使えるのは実質1.5日です。**
そのあと **8/26〜8/31 に First Stage の審査用動画を収録**します（要予約）。

担当は `A` / `B` / `C` で仮置きしています。**実名に置き換えてください。**

> ⏰ **日別の時間割は [`docs/10-roles-schedule.md`](10-roles-schedule.md) が最新です。**
> このファイルの「8/20（木）＝今日」以下の日付は書かれた時点のもので、**ずれています。**
> 各タスクの中身（何を確認するか・何に注意するか）はここが正、
> **いつ誰がやるかは `10-roles-schedule.md` が正**として使ってください。

> ⚠️ **8/22 朝の状況：テーマ未定・狙う賞未定・`src/` は空です。**
> 当初の計画では 8/19 にテーマを決めているはずでした。**3日遅れています。**
> **8/22 の午前中に決めないと間に合いません**（`docs/10-roles-schedule.md`）。

### 🟢 テーマ確定：候補4「小1の壁を、引っ越す前に知る」【2026-08-22】

要件定義と基本設計は完了しています。**3人は詳細設計から分かれます。**

| ドキュメント | 中身 |
|---|---|
| [`docs/13-requirements.md`](13-requirements.md) | 要件定義（スコープ・FR/NFR・**受け入れ条件**） |
| [`docs/14-basic-design.md`](14-basic-design.md) | 基本設計（**指標プラグイン**・予測エンジン・生成AI3か所・画面） |
| [`docs/15-interfaces.md`](15-interfaces.md) ＋ `src/types.ts` | **3人の境界**。ここが合意できたら並行実装開始 |
| [`data/SOURCES.md`](../data/SOURCES.md) | データ定義書 兼 **提出項目⑤の転記元** |
| `data/sample.json` | 実測値ベースのダミー（6自治体18校）。**②③はこれで先に書ける** |

#### 分かれる前に全員で確認すること（11:15–12:00）

- [ ] 全員：`docs/13-requirements.md` §4 スコープの**「やらないこと」に合意**する
- [ ] 全員：`src/types.ts` を読んで**自分の入力と出力が分かる**（`docs/15-interfaces.md` §6）
- [ ] ②：`src/types.ts` のオーナーとして起票・以降は勝手に変えない運用を宣言
- [ ] 全員：`docs/10-roles-schedule.md` の①②③に**実名を記入**する

#### ① データ担当

- [ ] `scripts/fetch.py`：8データセット＋係数2本 → `data/raw/`（**CP932→UTF-8で保存**）
- [ ] **`data/raw/` をコミットする**（疎通しないメンバーでも動かせるように）
- [ ] **生成AI①**：抽出プランを生成 → `scripts/plans/*.json` に**固定してコミット**
- [ ] `scripts/normalize.py` → `data/app/data.json`（`AppData`形式・**1ファイルに集約**。2026-08-22 ②と合意）
- [ ] **生成AI③**：注記生成 → 同ファイルの `munis[].note` に統合（**江戸川区・中央区・島嶼部を区別**）
- [ ] 🔴 **こども家庭庁の学童データ（複数年度）をローカルで15分確認**
      → 取れれば `Scenario.trend` を実測値に差し替え（`data/SOURCES.md`）
- [ ] `data/SOURCES.md` を最終確定（**ライセンスをAPIで再確認**）
- [ ] **出荷オーナー**：提出フォーム①〜⑥

#### 🔴 ③から②への依頼（第2版）→ **依頼文は [`docs/17-setup-request.md`](17-setup-request.md)**

> ⚠️ **第1版（セットアップ依頼）は破棄しました。** ②が先に `package.json` /
> `tsconfig.json` / `vite.config.ts` / `vitest.config.ts` を作り終えていたためです
> （Vite 8.2.2 / TypeScript 7。`@vitejs/plugin-react` 6.1.0 の peer `vite ^8` を満たすので
> 調整は不要でした）。**②の src/core/ は実装完了・34テスト green・マージ済みです。**

**②の Claude Code にそのまま貼れる形**にしてあります。**依頼は3件だけ**です。

- [x] 🔴 **依頼A【blocking】`entryYearOf(birthYear, birthMonth?)`** ✅ 対応済み（`abcdae0`）
      → `isEarlyBirth` / `entryAgeOffset` も公開され、`birthYearOf` も対称になった。
      **③の暫定ラッパー `src/ui/entryYear.ts` は削除済み**
- [x] 依頼B `MuniDetail.series` に **`demandBand`** ✅ 対応済み。`band × targetRate`。
      `band` 側にも「これは児童数の帯であって需要の帯ではない」と明記された
- [x] 依頼C `startup` という空の gitlink を消す ✅ 対応済み（`.gitignore` にも追加）

**依頼しないと決めたもの**（②の時間を使わせないため意識的に降ろしました）：
`HeatmapCell` への `demand`/`supply`/`gap` 追加（`CoreCell.detail` にある）／
`cells` の Map 化（`byMuni` がある）／`Heatmap.bins`（③が固定値で所有）／
`score` の丸め（③が表示時に）／`alternatives` の変更（③が地理的隣接で作り直す）

#### 🔴 ③から①への依頼 → **依頼文は [`docs/18-request-to-data.md`](18-request-to-data.md)**

- [ ] 🔴 **依頼1【blocking】`data/app/data.json` を49自治体ぶん**
      （`official[]` / `entrants[]` / `baseChildren` / `children2023` が未整備。
      いまは6自治体の `sample.json` で動いていて、**地図の43自治体がグレー**です）
      → 受け入れ検査は②の `validateAppData(data)` を使うこと
- [ ] **依頼2 生成AIを前処理で1回だけ回して結果をコミット**
      （用途① 様式不定CSVの読解ETLプラン／用途③ 制度差・外れ値の注記文）
      → **提出項目③「生成AI等の活用方法」に書ける実装が、いまここしかありません**

#### ② ロジック/AI担当

- [x] Vite ＋ TypeScript 雛形 ✅
- [x] `src/core/forecast.ts` ✅（学校別コーホート＋raking は未実装だが、
      出力は定義上ここの公式値と一致するのでスコアは変わらない）
- [x] `src/core/indicators/gakudo.ts`：登録率ベースの指標 ✅
- [x] 🔴 `excluded: true` を色の計算に入れない ✅（③側でも守っている）
- [x] バックテスト ✅（`src/core/bands.ts`。実測できるのは1年先と2年先だけ）
- [x] ~~`src/api/scenario.ts`~~ → **v1 では作らない。`PRESET_SCENARIOS` 5件に置き換え**
      （$100上限とライセンス発行待ちに作品を依存させないため。`docs/05-tech.md`）
- [x] **③からの変更依頼3件** ✅ すべて対応済み（`abcdae0`）
- [ ] **出荷オーナー**：デプロイと公開URL（**18:00–20:00 に初回デプロイを試す**）
      → 手順は [`docs/19-deploy.md`](19-deploy.md)。
      🔴 **Workers ではなく Pages です**（`npx wrangler pages deploy dist`）。
      🔴 **本番のプロジェクト名 `sho1-no-kabe` は予約済み。誰も取らないこと**
      （`*.pages.dev` は後から変更できず、他アカウントと衝突すると弾かれます）

#### ③ UI担当

- [x] **チャートを書く前に `dataviz` スキルを読む** ✅（配色は検証器にかけた）
- [x] ヒートマップ（`bridged` の境界に縦線と説明） ✅
- [x] 自治体詳細（需要と受け入れ実績の折れ線＋**需要の**予測区間・待機児童数） ✅
- [x] **注記の出し分け**（制度が別枠／本物の不足／母数不足） ✅
- [x] **打てる手**のセクション（地理的隣接＋スコア） ✅
- [x] 🔴 **出典・ライセンス・取得日を画面に出す** ✅（S5。JS無効時も `<noscript>` に）
- [x] 地図コロプレス ✅（拡大率1.8以上で自治体名ラベル）
- [x] スクロール構成 S0〜S5。**箱を使わない** ✅
- [x] 地図の拡大縮小・移動（`viewBox` 書き換え。d3-zoom は入れない） ✅
- [x] ランキングリスト（面積バイアスの補正役） ✅
- [x] ~~`/api/scenario` が落ちてもヒートマップが表示され続ける~~ → **API自体を作らないので該当なし**
- [ ] 提出フォームを開いて記入項目を実際に見る
- [ ] **③のアカウントで `sho1-no-kabe-preview` にリハーサル公開して実機で動確**
      （wheelズーム・ドラッグ・スマホ幅・初回ロードはブラウザでしか確かめられない）。
      手順とチェックリストは [`docs/19-deploy.md`](19-deploy.md)。
      **結果は同ファイル §5 に記録して push**（①の本番デプロイの先回り情報になる）。
      **終わったらプロジェクトを削除**する
- [ ] ①の `data/app/data.json` が来たら `src/ui/data.ts` の import を1行差し替える
- [x] ②の依頼Aが入ったので `src/ui/entryYear.ts` を削除し core を直接呼ぶようにした ✅
- [ ] **出荷オーナー**：スライド・キャプチャ・動画台本

#### 受け入れ条件（DoD）

**[`docs/13-requirements.md`](13-requirements.md) §9 の9項目**を全部通してください。

---

### ⛔ すでに過ぎている締切（事務局 Slack より）

| 締切 | 内容 | 影響 |
|---|---|---|
| **8/17（月）23:59** | ハッカソン本番 参加確認フォーム | **未登録なら現地参加できません。**ただし**提出は可能**（下記） |
| 8/16（日） | 生成AI実践ハンズオン（8/19）申請 | 終了 |
| 8/14（金） | 託児サービス申込み | 終了 |

**未登録でも作品提出はできます。** 事務局の明言：
「ハッカソン本番へのご参加自体は必須ではありません。**最終的に作品提出期限までに
作品をご提出いただければ、審査対象となります**」。

→ **現地に行けるかを今日確認してください。** 行けないなら3人ともオンライン前提で
段取りを組み直します（Zoomブレイクアウトルームは当日 `#03_事務局への質問` で依頼可）。

### 🔴 Cloudflare特典が間に合わない可能性【今日確認】

事務局 Slack によると、**Cloudflare のライセンスは2回上限に達しており、
「申請から発行まで1週間程度かかる見込み」**とのことです。

- **未申請なら、8/23 に間に合わない可能性が高い**
- 招待メールを受け取っている場合、**2週間以内に登録しないと招待が解除**されます
  （解除の個別連絡はなし。解除後は再申請が必要）
- 申請は**チーム単位**で、代表者1名が全員分をまとめて1回

→ **今日、申請状況と招待メールの受信を確認してください。**
間に合わないなら**デプロイ先を変える**判断が必要です（`docs/05-tech.md`）。

---

## 8/20（木）＝今日 — 決めて、骨組みまで【最重要】

**この日に決めないと間に合いません。**

### 午前〜昼：決める

- [ ] 全員：[`docs/09-shortlist.md`](09-shortlist.md) を読む
      — 実測済みデータに基づく**候補7案**と、実測で落ちた案の記録
- [ ] 全員：**各自で採点**してから持ち寄る（採点表は同ファイル内）
      — 1人が主導すると視野が狭くなります
- [ ] チーム：**テーマを1つに決定**
- [ ] チーム：**狙う賞を決定**
      — 平均点狙いは弱い。**どれか1つで突出させる**（`docs/04-ideas.md`）
- [ ] 決定内容を `docs/09-shortlist.md` の「決定事項」欄と **`CLAUDE.md`** に反映してコミット
- [ ] チーム：**3人の担当を確定** → [`docs/10-roles-schedule.md`](10-roles-schedule.md) の「割り当て」表に実名を入れる

### 午後：動き出す

- [ ] B：**決まった案のデータだけを前処理して `data/` にコミット**
      — 落とし穴は `docs/03-data.md` の「実測したデータの中身と落とし穴」に記載済み。
      **CP932・ヘッダ行の位置・空行・301リダイレクトを先に潰す**
- [ ] B：`data/SOURCES.md` を作る（**データセット名・URL・ライセンス・取得日**）
      — 提出フォームの「オープンデータの利用状況（最大10件）」にそのまま転記できる形で
- [ ] A：`docs/05-tech.md` の技術構成を確定 → プロジェクト雛形、`npx wrangler dev` が動くまで
- [ ] C：**プレゼンスライドの骨子**を作る（実装を待たない）
- [ ] 全員：担当ディレクトリを宣言して `CLAUDE.md` に記録
      — コンフリクト対策の要（`docs/07-team-workflow.md`）

### 今日中に確認（まだなら）

- [ ] 誰か1人：**Cloudflare特典の申請状況を確認**（上記の🔴参照）
      — 未申請なら今日出す。招待メール受信済みなら**今日登録する**
- [ ] 誰か1人：**現地参加できるかを確認**（8/17の参加確認フォーム提出済みか）
- [ ] 全員：**作品提出フォームを開いて記入項目を実際に見る**
      — https://form.jotform.com/261870604352051
      — **First Stage 収録枠の予約もこのフォームから**行います（30分単位・早い者順）
- [ ] 誰か1人：**提出ガイドを読む**（プレゼン資料の構成・画面キャプチャ・
      著作物利用の注意点。Google Map のロゴと帰属表示は隠さない・改変しない）
- [ ] 各自：自分のセッションで**疎通チェック**（`docs/07-team-workflow.md`）
      — 環境設定はアカウントごとです

## 8/21（金）— 動く状態にする

- [ ] A/B：**コア機能が一つ通しで動く**状態にする
      — 完璧でなくていい。「一番見せたい部分」が動くことを優先
- [ ] C：スライドのドラフト完成
- [ ] C：**エントリー項目①〜⑥の下書き**（各300字 × 6 ＝ 約1,800字）
      — 当日に書き始めると詰みます。項目一覧は `docs/01-event.md`
- [ ] 全員：**ローカルで動くことを確認**。この時点で動かないと本番が苦しい
- [ ] 誰か1人：Cloudflare に一度デプロイしてみる（ローカルから `npx wrangler deploy`）
      — 本番前日にデプロイの初トライをするのは危険。今日やっておく

## 8/22（土）— Day1【10:00–20:00】

**10時間あります。** 夜まで使えるので、Day2 は仕上げに専念できる状態を目指す。
公式は当日の概要を「詳細未定」としているので、メンタリング等の有無は現地で確認。

- [ ] 午前：残タスクの洗い出しと分担
- [ ] 午後：**コア機能の完成**に集中
- [ ] **17:00：中間チェック。** ここまでの状態を必ずコミット＆プッシュ
      — 以降にデグレしても戻れるようにしておく
- [ ] 夕方〜夜：スライドと実装のズレを解消、エントリー項目の下書きを詰める
- [ ] **20:00 撤収前に必ずコミット＆プッシュ**

## 8/23（日）— Day2 ＋ 提出【**17:00 提出締切**・10:00–17:00】

**7時間しかありません。** 提出作業に2時間は見ておくこと。

> ⚠️ 事務局：「**締切直前はサーバーが混み合い、状況によっては提出できない
> 可能性もございます。ぎりぎりの提出は避け、十分な余裕を持ってご提出ください**」
>
> ✅ **提出後の再編集は可能です**（初回登録時のメールに再編集URLが記載されます）。
> **まず早めに一度出して、あとから直す**のが安全です。

- [ ] 午前：**機能追加はここで打ち切る**。以降はバグ取りと仕上げのみ
- [ ] 昼：デプロイして**公開URLで動作確認**
- [ ] 午後：**作品提出**（フォームの必須項目は事前に確認しておくこと）
  - [ ] エントリー項目①〜⑥（各300字程度）
  - [ ] **オープンデータの利用状況 — 最大10件のリスト**（`data/SOURCES.md` から転記）
  - [ ] プレゼン資料（PowerPoint or PDF）＋ 画面キャプチャ最大3点
  - [ ] デモURL または1分操作動画（任意）
  - [ ] プロトタイプが公開URLで動くこと
- [ ] **提出完了を全員で確認**
- [ ] スライドの最終版を確定
- [ ] 17:30–19:00 クロージングイベント（参加者交流会）※会場のみ

> ⚠️ **午後に機能を足さない。** ハッカソンで一番多い失敗は、締切直前の変更で
> 動いていたものが壊れることです。

## 8/26（水）〜8/31（月）— First Stage 審査用動画撮影

**6日間。各チーム2分間のプレゼンを収録します。**

| 日付 | 形式 |
|---|---|
| 8/26（水）〜8/28（金） | オンライン（Zoom） |
| **8/29（土）** | **都内スタジオ / オンライン** ← スタジオを選べる唯一の日 |
| 8/30（日）〜8/31（月） | オンライン（Zoom） |

- [ ] **収録枠を予約**（予約方法・締切は要確認 → `docs/06-open-questions.md`）
      — 現地スタジオで撮るなら **8/29 の一択**。枠が埋まる前に押さえること
- [ ] 台本を作って**時間を計る**。2分は思ったより短い
- [ ] **審査基準5項目を意識した構成にする**（特に狙う賞の項目）
- [ ] 1チームの枠は**30分**（公式スケジュールの「いずれかの内30分」）。
      2分の収録に30分なので、撮り直しの余地はありそうです
- [ ] 「オープンデータをどう活かしたか」は必ず入れる（データ活用＝審査基準の筆頭）
- [ ] 収録完了

> ⚠️ **収録映像は YouTube で一般公開されます。** 内輪の審査用ではありません。
> 公開される前提で内容と見せ方を作ってください。

> 6人の審査委員がこの動画等をもとに審査し、24作品が選出されます。
> **結果発表は9月下旬頃**、作品集ページで全作品情報と審査結果が公開されます。

## 10/17（土）— Final Stage

- [ ] 進出できたら最終プレゼンの準備（**YouTube配信**・時間未定）
      — 当日の視聴者投票でオーディエンス賞が決まるので、事前の告知も効きます

---

## 役割分担の目安

> ⚠️ **この表は「1人が実装／1人がデータ／1人が企画」という古い割り方です。**
> **3人とも開発する**方針に変わったため、実際の分担は
> [`docs/10-roles-schedule.md`](10-roles-schedule.md) の
> 「開発担当 ＋ 出荷オーナー」の表を見てください。以下は参考として残します。

固定ではありません。キックオフで各自の得意分野に合わせて調整してください。

| | 主担当 | 具体的に |
|---|---|---|
| **A** | 実装・インフラ | アプリの骨格、Cloudflare まわり、デプロイ |
| **B** | データ | データセット選定・取得・整形、出典とライセンスの管理 |
| **C** | 企画・プレゼン | ストーリー設計、スライド、動画、UI |

**ただし全員が「8/23に提出する」ことに責任を持ちます。**
Cの担当だからといってスライドを1人で抱えない、Aが詰まったらBが見る、という前提で。

---

## 進捗の見方

このファイルのチェックボックスを更新して、**コミットしてください。**
3人の現在地がこれで揃います。
