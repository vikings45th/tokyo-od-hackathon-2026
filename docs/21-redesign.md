# リデザイン設計書 — 1本の縦スクロール（読む → 使う → 確かめる）

**2026-08-23 ／ ③UI担当（第3版）**
対象：`src/ui/App.tsx` ／ `src/ui/styles.css` ／ `src/ui/hooks.ts`（＋ `Series.tsx` `Choropleth.tsx` `data.ts`）
上位：[`13-requirements.md`](13-requirements.md) ／ [`14-basic-design.md`](14-basic-design.md) ／
[`16-ui-detail-design.md`](16-ui-detail-design.md) ／ [`20-service-canvas.md`](20-service-canvas.md)

---

## 0. 版の経緯

| 版 | 何をして、何が問題だったか |
|---|---|
| **旧** | S0〜S5 の6セクション。`min-height:100vh` の反復・同型セクション・触れる面が5番目で、**「パワポをPDFにしたもの」**に見えていた |
| **第1版** | 固定ヘッダ＋左に凍結した計器列＋右に流れる解説列。パワポっぽさは消えたが、**行き過ぎてダッシュボードになった**（「左が止まって右だけ動く構造が分かりにくい」「LPと道具が混在してぶれている」） |
| **第2版** | 凍結ペインを捨てて**1本の縦スクロール**に。モード転換の合図は「幅」。蝶番は生まれ年入力 |
| **第3版（この版）** | ①**地図クリックのバグを修復** ②**地図を全幅にして一番の見せ場に** ③**「どこに住めばいいか」を答える `#choose` を新設** |

### 第3版のきっかけ（実機を触っての指摘）

1. 地図上で自治体をクリックしても、表などに反映されない
2. 地図は一番の見せ場なので大きく見せたい。横の帯をどこに置くか
3. 「学童に入れないのはどこか」より「どこに住めばいいか」をサジェストしたい

---

## 1. デザインコンセプト

> **1本の縦スクロールで、読者を「読む」から「使う」へ手渡す。**
> モード転換の合図は**幅**。凍結ペインは使わない。

```
.read  660px 中央寄せ  #lede → #why → #ask                          読む
─────────────────────────────────────────────────  ← ここで幅が変わる
.wide 1360px          #tool → #choose → #muni → #heat → #scenario   使う
─────────────────────────────────────────────────  ← ここで幅が戻る
.read  660px          #evidence → #sources                          確かめる
```

幅が変わること自体が「ここから道具です」の宣言になる。**切り替わりは1往復だけ**
（読む → 使う → 読む）。交互に出すと元の「同型反復」に戻る。

**蝶番は `#ask` の生まれ年入力。** ここで「一般の話」が「あなたの話」になる。
`docs/20-service-canvas.md` の検査項目「誰にでも当てはまることを書くな／常に読者の固有の
状況に接地させる」が、そのまま構造になっている。

---

## 2. 「パワポっぽさ」と「ダッシュボードっぽさ」を、どちらも避けている根拠

| 避けたいもの | どう抑えているか |
|---|---|
| **1画面＝1枚（めくる感じ）** | `min-height:100vh` を作らない。`#lede` だけ `52vh` で、ファーストビューに必ず次のセクションの頭が入る |
| **同型セクションの反復** | 型は `.read`（本文幅・散文）と `.wide`（全幅・図版）の2つだけ。`eyebrow → 見出し → リード → 中身` の反復はやめ、大見出しは各セクション1本（`h1` は `#lede` のみ） |
| **触れる面が埋もれる** | 固定ヘッダに主入力と「地図で調べる ↓」。`#ask` の蝶番。`#tool` はスクロール2回で着く |
| **スクロール連動の演出** | `.rise`（フェードイン）も、スクロールで年度が進む物語も全廃 |
| **ダッシュボード（凍結ペイン）** | sticky を1つも使わない。`#tool` の地図も普通に流れる |

---

## 3. 各セクションの目的と内容

### `.bar` — 固定ヘッダ（58px／スマホ84px2段）
ペインではなく**ツールバー**。サービス名／**子の生まれ年月の選択**／`→ 小1は ◯◯年度`／
早生まれ注記／`地図で調べる ↓`／`精度`／`出典`／テーマ切替。

### 読む（`.read` 660px）

| id | 目的 | 中身 |
|---|---|---|
| `#lede` | 何の話かを2文で。**表紙にしない**（`min-height:52vh`） | h1「保育園には、入れた。／小1の壁は、どこにある？」＋リード＋**ライブ年表**（2026 →〈N年〉→ 小1の年）＋射程外の警告 |
| `#why` | なぜ「いま」わからないのか | ①不動産サイトも区役所も答えるのは「今年の」待機児童数だけ ②待機児童数では順位が付かない（**◯自治体のうち◯が待機ゼロ**・実行時に数える）＋申込前に諦めた人は計上されない ③CSVはヘッダ6行目・区部と市町村部が横に2ブロック・数値は `"1,261 "` 形式 |
| `#ask` | **蝶番。** 一般の話を、あなたの話にする | 「あなたの子は、何年生まれですか。」＋大きめの生まれ年月の選択＋`→ 小1になるのは ◯◯年度`＋`◯◯年度の東京49自治体を見る ↓` |

### 使う（`.wide` 1360px）

#### `#tool` — 地図が一番の見せ場

```
secthd:  h2「{viewYear}年度、東京49自治体。」 ......... [◯◯の周辺へ][23区へ][全体]
.mapstage   全幅 × min(66vh,700px)。SVG は中で meet して中央に収まる
.legend     凡例（左）................................. 操作ヒント（右）
.selbar     ◯◯区 {viewYear}年度 ｜ 882 人が、入れない ｜ 必要／受入／待機／クラブ
.scrub      年スクラバ（掴める・2027〜2038）
.minisrc    出典1行（FR-8）
```

- **地図の実寸**：1440×900 で 約1140×600（第2版の 930×537 から線寸で2割強の拡大）。
  `min(66vh,700px)` で高さを頭打ちにしているのは、**地図と `.selbar` が1画面に収まるため**。
  幅なりに描くと 1360×786 になり、クリックの結果が確実にフォールドの下へ落ちる
- **ズームボタンは地図に重ねない**（`secthd` の右）。拡大・移動すると地形と重なって読めなくなるうえ、
  「箱を使わない」方針とも衝突する
- **`.selbar` は年スクラバより前**。スクラバは自分で探しに行く操作子だが、
  `.selbar` はクリックの**フィードバック**なので、必ず見える位置に置く

#### `#choose` — 「じゃあ、どこに住めばいいか。」（第3版で新設）

```
secthd: h2「じゃあ、どこに住めばいいか。」... meta「{firstYear}〜{lastYear}年度のどの年も不足しないのは ◯自治体」
┌ 左：{sel}の近くなら ──────────┬ 右：全員入れる期間が長い順 ─────────┐
│ 選択自治体（いま見ている）＋      │ 上位10。ボタンで「いま厳しい順」に切替  │
│ 隣接自治体を全件、良い順に        │                                     │
│ 行＝自治体名／最初に不足する年度／ │ 行＝順位／自治体名／最初に不足する年度／ │
│    {viewYear}年度に入れない子（人）│    {viewYear}年度に入れない子（人）    │
└───────────────────────────┴─────────────────────────────┘
注記：この順位は学童の受け皿だけを見たものです。通勤時間・家賃・保育園は含んでいません。
```

**並べ替えの指標（第3版で新設）**

素直に「入れない割合が低い順」を出すと、**下位に 0% が大量に並んで順位にならない**
（上位は 41 / 13 / 12 …だが、下に行くほど 0 が連続する分布）。そこで：

> **最初に不足が出る年度** — その自治体で `detail.gap >= 1`（入れない子が1人以上）になる最初の年度。
> 最後まで出なければ「{lastYear}年度まで、どの年も全員入れる」。

同着は「最終年度のスコア昇順 → 最終年度の余裕人数（`supply − demand`）降順 → 名前」で割る。
これで 0% 同士にも差が付き、実測でも **{lastYear}年度まで全員入れるのは数自治体**まで絞り込める。

⚠️ 言い回しは **「最初に不足するのは ◯◯年度」**にしてある。「◯◯年度から先ずっと不足する」ではない
（需要は年で上下するので、いったん不足してから戻る自治体がある）。
また `Math.round(gap) >= 1` ではなく **`gap >= 1`** で判定している。0.6人を1人に丸めると
モデルの分解能を超えた主張になるため。

⚠️ 右端の数字は **% ではなく人数**。数人の不足は四捨五入で 0% になるので、
「最初に不足するのは2027年度」の隣に「0%」が並んで矛盾して見える。

> 🔴 **単軸しかないので「総合おすすめ度」は名乗らない。** 通勤時間も住宅価格も持っていない
> （`docs/13-requirements.md` §4 で意識的に捨てた軸）。画面に断りを必ず出す。

#### `#muni` `#heat` `#scenario`

| id | 中身 |
|---|---|
| `#muni` | 数字の**読み方**。注記（本物の供給不足／計上方法の変更／母数不足を区別）→ 需要と受け入れの折れ線＋予測区間 → 打てる手3つ（引っ越し先を変える以外の助言。※隣接自治体の比較は `#choose` に移した） |
| `#heat` | 左＝49自治体 × 12年度のマトリクス（既定は上位10件。**選択自治体が圏外なら先頭に固定**、`bridgeFrom` 列に縦罫、推定セルにハッチ、いま見ている年度の列に枠）／右＝表の読み方と推定区間の断り書き、展開ボタン |
| `#scenario` | プリセット5つ（それぞれ根拠つき）。スライダーは折りたたみの中。実測から外れたら警告 |

### 確かめる（`.read` 660px）

| id | 中身 |
|---|---|
| `#evidence` | バックテスト 1年先 **0.93%**／2年先 **1.37%**（n=49・10〜90パーセンタイルつき）、登録率トレンド **+0.81pt/年**（48自治体・24.0%→25.6%・江戸川区は計上方法変更のため除外）※すべて実行時に計算 |
| `#sources` | **ライセンスごとに束ねた**出典リスト、加工表記、**muni_code 結合と 49×12＝588セル**の説明 |

---

## 4. 配色・タイポ・余白

### 引き継いだもの（変更なし）

- **`src/ui/palette.ts` は1文字も触っていない。** 5段ランプ・ink・nodata すべて据え置き
- 配色トークン：`--accent #1c5cab`／地 `#ffffff #fcfcfb #f4f4f2`／文字 `#0b0b0b #52514e #8a8983`／
  罫 `#e6e6e1`／`--warn` `--crit`／ダークテーマ一式
- タイポ：`-apple-system, "SF Pro Display", "Hiragino Sans", "Noto Sans JP"`、本文15px/1.6、
  `font-feature-settings:"palt" 1`、数字は `tabular-nums`
- **「箱を使わない」**：枠線＋影のカードはゼロ。区切りは余白と1pxヘアラインのみ

**コントラスト制約は守っている。** 最淡 `#86b6ef` が載る面は地図もヒートマップも
`--bg-0 #ffffff`（2.11:1 PASS）。**セクション背景をグレーにしていない**
（`#ask` だけ `--bg-1 #fcfcfb`＝2.06:1 PASS。ここにランプ色は載せていない）。

### 骨格トークン

| トークン | 値 | 用途 |
|---|---|---|
| `--read` | `660px` | 読む幅。日本語で1行 約34文字 |
| `--wide` | `1360px` | 使う幅 |
| `--bar-h` | `58px`（≤1000で52／≤600で84） | 固定ヘッダ |
| `--gut` | `36px`（26 / 18 / 14） | ページ左右の余白 |
| `--fs-h1` / `--fs-h2` | `clamp(30,3.2vw,50)` / `clamp(21,2.1vw,30)` | `#lede` のみ h1 |
| `--fs-result` | `clamp(38px,3.4vw,54px)` | `.selbar` の数字 |

---

## 5. PC とスマホ

| 幅 | 方針 |
|---|---|
| **〜1440px（主戦場）** | `.read` 660／`.wide` 1360。地図は全幅 × `min(66vh,700px)`。`#choose` は 1fr 1fr、`#heat` は `minmax(0,880px) 300px` |
| **〜1280px** | `--gut` 26px。`#heat` 右柱 260px、`#choose` の gap 32px |
| **〜1000px** | `#choose` `#heat` を1列に。地図は `aspect-ratio:900/520`（vh で切ると上下に無駄な余白が出る）。**sticky は1つも使わない** |
| **〜600px** | ヘッダを2段（上＝名前とテーマ／下＝主入力）。390px幅では1行に入らないので、要素を消すのではなく段を増やす |

> 🔴 地図には `touch-action:pan-y` を当てている。`none` のままだと地図の上で
> 縦スワイプが吸われてページが動かない。横ドラッグでの地図パンは残る。

実機確認は**まだ取れていない**（`[要確認]`）。Chromium 390×844 / 1280×800 / 1440×900 の実測に基づく。

---

## 6. 実装

すべて `src/ui/` 内で完結。**新規依存ゼロ**（d3-geo と topojson-client のまま）。
`package.json` `vite.config.ts` `src/core/` `data/` `src/ui/palette.ts` には触れていない。

### 🔴 `src/ui/hooks.ts` — 地図クリックが効かなかったバグの修復

**症状**：地図上のどの自治体を押しても選択が変わらない（49件すべて）。

**原因**：`useZoomPan` が `pointerdown` の時点で `setPointerCapture` していた。
捕獲すると続く `pointerup` の target が `<path>` ではなく `<svg>` になる。
`click` は pointerdown と pointerup の**共通祖先**に発火するので `<svg>` で発火し、
自治体 `<path>` の `onClick` が一度も呼ばれない。**ドラッグ機能を足したときにクリックを潰していた。**

**修正**：動き始めてから捕獲する。

```ts
/** これ以上動いたらドラッグ。これ未満はクリック（自治体の選択）として扱う */
const DRAG_SLOP = 4;

const onPointerDown = (e) => {
  // 🔴 ここで setPointerCapture してはいけない（上記の理由）
  drag.current = { x: e.clientX, y: e.clientY, vx: vb.x, vy: vb.y, captured: false };
};
const onPointerMove = (e) => {
  const d = drag.current; if (!d) return;
  if (!d.captured) {
    if (Math.hypot(e.clientX - d.x, e.clientY - d.y) < DRAG_SLOP) return;
    e.currentTarget.setPointerCapture(e.pointerId);
    d.captured = true;
  }
  … 既存のパン処理 …
};
```

**検証済み**（Playwright・`vite preview`）：

| | 修正前 | 修正後 |
|---|---|---|
| 地図の自治体をクリック | 選択が変わらない ❌ | 中央区 → 千代田区 ✅ |
| `.selbar` / `#choose` 左 / ヒートマップ先頭行 / 地図の選択リング | — | すべて追従 ✅ |
| 拡大後に90px ドラッグ | viewBox が動く ✅ | viewBox が動く／選択は変わらない ✅ |

### `src/ui/App.tsx`

- `#tool` を再構成（地図を全幅 → 凡例 → `.selbar` → 年スクラバ → 出典1行）
- **`#choose` を新設**。`Hold` / `holdOf()` / `compareHold()` / `ChooseRow` を追加
- `heatRows`：選択自治体が上位10件の外なら、末尾に足すのではなく**先頭に固定**
- `#muni` から隣接自治体のチップを撤去（`#choose` に昇格）
- 見出し：`#tool` は「{year}年度、学童に入れないのはどこか。」→「{year}年度、東京49自治体。」

### `src/ui/styles.css`

- 削除：`.toolgrid` `.mapfig` `.result` `.ranktitle` `.rank/.rr` `.acts/.act`
- 追加：`.mapstage`（全幅 × `min(66vh,700px)`）／`.selbar`／`#choose` `.choosegrid` `.crow` ほか

### 壊していないこと

- `src/ui/palette.ts` — **無変更**
- `src/index.html` の `<noscript>` 出典 — **無変更**（JS無効時の FR-8 担保）
- ビルド時 import のみ。**実行時のネットワークアクセスはゼロ**（NFR-4）
- `npm run typecheck` / `npm run build` ともに通過

---

## 7. 時間があればやりたいこと

| やりたいこと | なぜ今回やらないか |
|---|---|
| **URLに状態を持たせる**（`?y=2024&m=6&muni=杉並区`） | 審査員に「この画面」を共有できると強い。実装30分だが、動画は操作を映すので効果が薄い |
| 自治体名の**検索ボックス** | 49件なら地図＋`#choose`＋ヒートマップで足りる。入力欄が増えると蝶番がぼやける |
| 2自治体の**並べ比較** | `#choose` 左の「近くなら」でほぼ代替できている |
| **実機確認**（iOS Safari / Android Chrome） | Chromium 実測のみ |

---

## 8. 未確定事項（`[要確認]`）

画面に出る数値は**すべて実行時に `data/app/data.json` と `src/core/` から計算**している。
UI側にハードコードした数値は1つも無い。そのうえで確定していないのは次の3点。

1. ~~同じ画面に「実測 +0.81pt」と「実測 +0.84pt」が並んで出ている~~
   → **✅ 解決済み。** ②が main で修正（`DEFAULT_TREND` を 0.0081 に訂正し、
   プリセットの説明文からは数字そのものを削除して二重に持たない形にした）。
   2026-08-23 に main をこのブランチへ取り込み、画面で +0.81pt に揃っていることを確認した。
2. **`[要確認]` 国土数値情報の利用約款とクレジット文言** — `data/SOURCES.md` §9 で①が確認中。
   `src/ui/data.ts` の `GEO_SOURCE.license` は暫定。**8/26 の撮影（YouTube 一般公開）前に確定させること**
3. **`[要確認]` スマホ実機での表示** — Chromium 390×844 のみ。iOS Safari は未確認

> 2026-08-23：`origin/main`（②の core 5コミット）を取り込み済み。API は追加のみで
> UI 側の変更は不要だった。`npm test` は **66件すべて通過**（取り込み前は3件失敗）。
> 画面に出る数値（中央区 2031年度 882人／必要2,173／受入1,291／待機275／クラブ31、
> 実測トレンド +0.81pt、バックテスト 0.93%・1.37%、どの年も不足しない3自治体）は
> 取り込み前後で一致している。
