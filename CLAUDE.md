# CLAUDE.md

このファイルは、チーム3人がそれぞれ Claude Code を使うときの**共通の前提**です。
リポジトリに置いてあるので、誰のセッションでも自動的に読み込まれます。
**決まったことは必ずここに反映してください。** 次のセッションの前提になります。

---

## このプロジェクトは何か

**都知事杯オープンデータ・ハッカソン2026** の参加作品を開発するリポジトリです。

東京都のオープンデータを活用して、東京都が抱える社会課題の解決に資する
デジタルサービスを企画・開発します。

- チーム人数：3人
- エントリー：**済み**
- 狙う賞：**未定** ← **8/22 午前中に決める**（`docs/10-roles-schedule.md`）
- テーマ：**未定** ← 候補7案は `docs/09-shortlist.md`。採点して選ぶ

## 動かせない締切

| 日付 | 何が起きるか |
|---|---|
| **8/22（土）** | ハッカソン Day1 **10:00–20:00**（ベルサール半蔵門 2Fホール／Zoom併用） |
| **8/23（日）** | ハッカソン Day2 **10:00–17:00** ＋ **作品提出締切 17:00**（交流会 17:30–19:00） |
| **8/26〜8/31** | First Stage 審査用動画撮影（2分・**要予約**・枠は6日間のみ） |
| 10/17（土） | Final Stage・表彰式 |

**8/23 を過ぎたら何もできません。** 実装の判断は常に「8/23 に間に合うか」で切ってください。

詳細は `docs/01-event.md`。

## 提出物

1. **プロトタイプ**（動くもの）
2. **プレゼンスライド**（PowerPoint or PDF）
3. **2分程度のプレゼン動画**（8/26〜8/31 に収録・要予約）
   — **YouTube で一般公開されます。** 内輪の審査用ではありません
4. **エントリー項目①〜⑥**（提出フォームへ入力・各300文字程度）
   — チーム情報／サービスの概要／プロダクト・技術詳細／チームの実行力／
   **オープンデータの利用状況（最大10件）**／プレゼン資料
5. デモURL または1分操作動画（任意）

実装だけ進んでスライドと文章が無い、という状態にしないこと。
**約1,800文字の記入欄**があるので、当日に書き始めると詰みます。

## 審査基準（5項目）

| 基準 | 見られること |
|---|---|
| **データ活用** | オープンデータや民間データ等を有効活用しているか |
| **アイデア力** | 課題解決へのアプローチが秀逸で、サービスとして成立しているか |
| **技術力** | 技術難易度・アーキテクチャやAI等の合理性・完成度 |
| **ソーシャルインパクト** | 手取り時間の増加等、都民のQOL向上。実現可能性 |
| **サービスデザイン** | 都民目線の価値を持続的に提供できるか |

**この5基準がそれぞれ賞になっています**（データ活用賞／アイデア賞／技術賞／
ソーシャルインパクト賞／サービスデザイン賞）。加えて都知事杯・学生賞・
オーディエンス賞の全8賞。**2026年度から刷新された構成**です。

審査するのは**6人の審査委員**（実名と所属は `docs/01-event.md`）。
Eukarya（Re:Earth）CEO・Zaim創業者・GovTech東京CTO・東京都副知事など、
**行政3人＋民間3人**の構成です。読み解きは `docs/04-ideas.md`。

→ **平均点狙いは弱い。** どれか1つで突出させる方が受賞確率が高い設計です。
機能を足す前に「これは狙う賞の基準を上げるか」を自問してください。

---

## 絶対に守ること

### 秘密情報をコミットしない

- APIキー・トークン・パスワードをリポジトリに入れない
- **クラウド環境の環境変数欄にも入れない。** 公式ドキュメントに
  「anyone who uses the environment can read the values」と明記されている
- `.env` は `.gitignore` 済み。`.env.example` にキー名だけ書く

### オープンデータの出典を明記する

作品内に**データの出典とライセンス表記**を必ず入れてください。
東京都オープンデータカタログのデータは原則 CC BY 4.0 です
（個別に別ライセンスのものがあるので `docs/03-data.md` で確認）。

出典明記は大会の趣旨そのものであり、審査観点にも直結します。

### 許諾が要るデータは、最初からダミーデータで作る

**事務局の方針です（Slack 2026-08-14）。**

| データの種類 | 扱い |
|---|---|
| **東京都オープンデータカタログ掲載データ** | ✅ **許諾不要**。原則 CC BY 4.0、商用利用も可 |
| CCライセンスが明記されているデータ | ✅ 許諾不要（個別規定があればそちらに準拠） |
| **上記以外の東京都データ**（都HPに「公表」されているだけのもの） | ⚠️ **開発開始時点からダミーデータで進める** |
| **区市町村・他都道府県のデータ** | ⚠️ **自分で利用規約を確認し、必要なら個別に許諾申請** |

**なぜダミーデータか**：First Stage（8/26〜）の動画は**一般公開**されるため、
そこで許諾のない利用が表面化します。「途中まで実データ、直前にダミーへ差し替え」は
ハッカソンから収録まで数日しかなく、手間もリスクも大きいためです。

> ⚠️ **「引用だから許諾不要」は通りません。**
> 著作権法上の引用は「自分の作品が主役で、引用部分は補足」という関係が必要です。
> **施設情報や医療機関リストをアプリの中核コンテンツとして使う場合は引用に当たりません。**

### 都庁各局に直接問い合わせない

データの利用可否を確認したいときは、**各局へ直接連絡せず、
まず事務局（Slack `#03_事務局への質問`）へ**。事務局経由の方が調整が進みます。
**これも事務局からの明示的な依頼です。**

### 個人情報を含むデータは扱わない

### 他人のファイルを勝手に触らない

3人が同時に Claude Code を動かします。担当ディレクトリの外を大きく書き換えると
即コンフリクトします。詳細は `docs/07-team-workflow.md`。

---

## この環境の制約（重要）

### ネットワークは環境設定次第（まず疎通チェックする）

クラウドセッションの既定は **Trusted** レベルで、この状態では
**GitHub とパッケージレジストリ以外すべて遮断**されます。東京都のサイトも
Cloudflare API も通りません。

ただし **2026-08-20 の疎通チェックでは、クラウドセッションから
東京都オープンデータに直接アクセスできました**（カタログのCKAN API・
ポータル・`www.opendata.metro.tokyo.lg.jp` のCSV実データ・Cloudflare API すべて到達）。
つまり環境がすでに緩められているケースがあります。

**環境設定はアカウントごとです。誰か1人が通っても他の2人には反映されません。**
セッションを立ち上げたら、まず `docs/07-team-workflow.md` の疎通チェックを
走らせて、自分の環境がどちらなのかを確認してください。

### Cloudflare へのデプロイはローカルから

`wrangler login` はブラウザOAuthなのでクラウドセッション内では完結しません。
**デプロイは各自のローカルマシンから `wrangler deploy`** で行ってください。
クラウドセッションは実装とテストに使います。

### データ取得はビルド時ではなく実行時に

疎通する環境・しない環境が混在するので、**ビルドが通るかどうかが
セッションのネットワーク設定に依存する構成は避けてください。**
3人のうち1人だけビルドできない、という事故になります。

設計としては次のどちらかにしてください：

- 取得済みの CSV / JSON を `data/` に置く（コミットして全員で共有）
- **ブラウザ実行時にクライアント側から取得**する

セッションから直接データを取れる場合は、それは
**「`data/` に置くファイルを作る作業」に使う**のが安全です。

---

## 作業の進め方

### 設計判断はプランモードで

方針が分かれる作業は、いきなり実装せず `claude --permission-mode plan` で
合意してから進めてください。3人が別々の前提で書き始めるのが一番の事故です。

### こまめにコミット＆プッシュ

クラウドセッションのVMは**放置すると回収されます**。
手元に残したいものは必ずプッシュしてください。

### ブランチ運用

**1人1本の作業ブランチ `work/<自分の名前>` を使い回します。**（作業単位ごとには切りません）
**レビューは挟みません。** 衝突は**ディレクトリ縦割り**（`docs/10-roles-schedule.md` §1）で防ぎます。

- 各自1回だけ：
  ```bash
  git config merge.autoStash true && git config rebase.autoStash true
  git config push.default current
  git config alias.catchup '!git fetch origin main && git merge origin/main'
  git switch -c work/<自分の名前>
  ```
- 作業ループ：`git catchup` → `git add -A && git commit -m "..."` → `git push`
  → push 時に出る URL から PR を作り、**自分で即 Merge**（承認待ちはしない）
- ⛔ **マージは必ず「Create a merge commit」。「Squash and merge」は使わない**
  — squash するとマージ済みの変更が「未マージ」扱いで残り、次の `catchup` で蘇って衝突します
- ⛔ **「Delete branch」を押さない**（使い回すため）。マージ後は必ず `git catchup`
- **1日3回（15:00 / 17:00 / 20:00）は必ず `main` へマージ。** 溜めると 8/23 に詰みます
- クラウドセッションの `claude/*` も手順は同じ。ただし**使い捨て**なので
  Delete branch も Squash も可。**その日のうちに `main` へ**
- 動いた瞬間に `main` の上で `git tag`（`day1-1700` など）。
  壊れたら `git checkout <tag> -- <パス>` で戻す
- **`git push --force` は禁止。** 3人が `main` を共有しているので他2人の作業が消えます

詳細は `docs/07-team-workflow.md` §3-2。

### レート制限はアカウント全体で共有

並列セッションを増やすとその分消費が早くなります。

---

## ディレクトリ構成

```
docs/          調査結果・計画・運用ルール（まずここを読む）
data/          取得済みオープンデータ（CSV/JSON）
src/           アプリケーション本体（構成は docs/05-tech.md で決定後）
```

## ドキュメント一覧

| ファイル | 内容 |
|---|---|
| `docs/01-event.md` | 大会情報（審査基準・賞・日程・提出物） |
| `docs/02-todo.md` | 期日ごとのタスクリスト（**日別の時間割は `10-roles-schedule.md` が最新**） |
| `docs/03-data.md` | データソース調査（カタログ・API・ライセンス） |
| `docs/04-ideas.md` | アイデア出しと意思決定（有力3方向・評価マトリクス） |
| `docs/05-tech.md` | 技術方針 |
| `docs/06-open-questions.md` | 一次情報で確認すべき未確認事項 |
| `docs/07-team-workflow.md` | 環境セットアップと3人運用ルール |
| `docs/08-themes.md` | 東京都提示テーマ全一覧（12カテゴリ・57件） |
| `docs/09-shortlist.md` | **テーマ候補7案のたたき台**（実測済みデータ・採点表・足切り記録） |
| `docs/10-roles-schedule.md` | **役割分担と当日スケジュール**（3人とも開発する前提の割り方・時間割） |
| `docs/11-ai-log.md` | AI活用ログ（提出項目③「生成AI等の活用方法」の材料。その場で1行ずつ書く） |
